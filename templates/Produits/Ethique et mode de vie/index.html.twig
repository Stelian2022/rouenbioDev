{% extends 'base.html.twig' %}
{% set navigationItems = [
    { 'label': 'Accueil', 'link': '/' },
    { 'label': 'À propos', 'link': '/a-propos' }
] %}
{% block content %}
	<div class="newPage">
		<h1>{{currentPage}}</h1>
	</div>
	{% for label, url in emdv %}
		<div id="submenu-cards" class="card-container">
			<div class="card">
				<li>
					<a href="{{ url }}">{{ label }}</a>
				</li>
			</div>
		</div>
	{% endfor %}
{% endblock %}
