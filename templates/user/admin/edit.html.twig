{% extends 'base.html.twig' %}
{% set navigationItems = [
    { 'label': 'Accueil', 'link': '/' },
    { 'label': 'Deconnexion', 'link': '/logout' }
] %}


{% block content %}
	<link rel="stylesheet" href="{{ asset('assets/styles/general/form.css') }}">


	<div class="newPage">
		<h1>{{currentPage}}</h1>
	</div>
{% if is_granted('ROLE_ADMIN') %}
		<a href="{{ path('admin_clients') }}" class="button">Retour à la liste</a>
		{{ include('user/admin/_delete_form.html.twig') }}
	{% else %}
		<a href="{{ path('profile') }}" class="button">Retour au profil</a>
	{% endif %}

	{{ include('user/admin/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
{% endblock %}
